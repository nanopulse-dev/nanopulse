# PIN

A short secret that is known by both the Device and Server. This is used to
identify the Device during the exchange of Device and Server public-keys.
Internally, the _PIN_ is a sequence of bytes. It is printed as HEX-string when
presented to the user.
